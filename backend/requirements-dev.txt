-r requirements.txt

# httpx is a test dependency only (fastapi.testclient).
# Move it into requirements.txt when the /chat LLM client lands.
pytest==8.3.4
httpx==0.28.1

ruff==0.8.4
mypy==1.13.0
bandit==1.8.0
